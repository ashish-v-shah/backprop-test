# This is an example environment configuration file for the Node.js Hello World application
# Copy this file to .env in the same directory and modify values as needed
# The .env file should not be committed to version control

# TCP port on which the server listens for incoming HTTP requests
# Must be a positive integer between 1 and 65535
PORT=3000

# Host address to bind the server to
# Use 0.0.0.0 to listen on all interfaces or 127.0.0.1 for localhost only
HOST=0.0.0.0

# Application environment (development, production, test)
NODE_ENV=development

# Logging verbosity level
# Valid values: error, warn, info, debug
LOG_LEVEL=info